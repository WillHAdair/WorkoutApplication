class UserProfile {
  int id;
  double height;
  double weight;
  DateTime lastUpdated;

  UserProfile({
    required this.id,
    required this.height,
    required this.weight,
    required this.lastUpdated,
  });
}